*.iml
.gradle
/local.properties
# Play Store upload-signing keystore -- generated locally (see build.gradle.kts's signingConfigs),
# never committed: losing it just means generating a new one and re-registering the upload key
# with Google, but a leaked one lets someone else sign app updates that look legitimate.
/release.jks
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
video/
